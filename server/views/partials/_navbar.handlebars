<div class="navbar-fixed">
  <nav class="black">
    <div class="container">
      <div class="nav-wrapper">
        <a href="#" class="brand-logo"><img src="/img/sugoi_sombrio_custom.png"><small>v2.0.2</small></a>
        <a href="#" data-target="mobile-nav" class="sidenav-trigger"><i class="material-icons">menu</i></a>
        <ul class="right hide-on-med-and-down">
          {{#if user}}
          <li>
            <a href="/orders"><i class="fas fa-box-open"></i> Orders</a>
          </li>
          {{#if user.viewProd}}
          <li>
            <a href="/prod"><i class="fas fa-calendar-check"></i> Production</a>
          </li>
          {{/if}}
          {{#if user.editOrders}}
          {{!-- {{#unless user.lgUser}} --}}
          <li>
            <a href="/payments/outstanding"><i class="fas fa-file-invoice-dollar"></i> Payments</a>
          </li>
          {{!-- {{/unless}} --}}
          {{/if}}
          <li>
            <a class='dropdown-trigger btn' href="#" data-target="dropdown1" style="width: 180px;">{{getHandle
              user.username}}</a>
          </li>
          <ul id="dropdown1" class="dropdown-content">
            {{#if user.admin}}
            <li>
              <a href="/admin/dash"><i class="material-icons">supervised_user_circle</i>Admin Dashboard</a>
            </li>
            {{/if}}
            <li>
              <a href="/users/password"><i class="material-icons">vpn_key</i>Change Password</a>
            </li>
            <li>
              <a href="/users/logout"><i class="material-icons">power_off</i> Logout</a>
            </li>
            {{/if}}
          </ul>
        </ul>
      </div>
    </div>
  </nav>
</div>
<ul class="sidenav" id="mobile-nav">
  {{#if user}}
  <li class="center-align">
    {{#if user.admin}}
    <div class="chip">
      <i class="fas fa-user-shield"></i>
      {{user.username}}
    </div>
    {{else}}
    <div class="chip">
      <i class="fas fa-user"></i>
      {{getHandle user.username}}
    </div>
    {{/if}}
  </li>
  {{/if}}
  {{#if user}}
  <li>
    <a href="/orders"><i class="material-icons">color_lens</i>Orders</a>
  </li>
  {{#if user.viewProd}}
  <li>
    <a href="/prod"><i class="material-icons">
        assignment_turned_in
      </i> Production</a>
  </li>
  {{/if}}
  {{#if user.editOrders}}
  {{!-- {{#unless user.lgUser}} --}}
  <li>
    <a href="/payments"><i class="material-icons">attach_money</i>Payments</a>
  </li>
  {{!-- {{/unless}} --}}
  {{/if}}
  <li class="divider"></li>
  {{#if user.admin}}
  <li>
    <a href="/users/admin/dash"><i class="material-icons">supervised_user_circle</i>Admin Dashboard</a>
  </li>
  {{/if}}
  <li>
    <a href="/users/password"><i class="material-icons">vpn_key</i>Change Password</a>
  </li>
  <li>
    <a href="/users/logout"><i class="material-icons">power_off</i>Logout</a>
  </li>
  {{else}}
  <li>
    <a href="/users/login"><i class="fas fa-sign-in-alt"></i> Staff Login</a>
  </li>
  {{/if}}
</ul>